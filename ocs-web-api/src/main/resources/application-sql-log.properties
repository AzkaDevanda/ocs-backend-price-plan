# Optional profile for temporary SQL investigation in production/staging.
# Enable with: --spring.profiles.active=prod,sql-log
app.logging.request.enabled=true

decorator.datasource.enabled=true
decorator.datasource.p6spy.enable-logging=true
logging.level.p6spy=INFO
