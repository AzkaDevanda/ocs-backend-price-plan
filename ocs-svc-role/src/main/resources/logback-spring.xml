<configuration>

  <!-- ========================= -->
  <!-- ENV CONFIG -->
  <!-- ========================= -->
  <!--  <property name="LOG_PATH" value="${LOG_PATH:-/apps/logs}" />-->
  <property name="LOG_PATH" value="${LOG_PATH:-logs}" />
  <property name="LOG_RETENTION_DAYS" value="${LOG_RETENTION_DAYS:-180}" />
  <property name="LOG_MAX_TOTAL_SIZE" value="${LOG_MAX_TOTAL_SIZE:-10GB}" />
  <property name="LOG_MAX_FILE_SIZE" value="${LOG_MAX_FILE_SIZE:-100MB}" />

  <!-- ========================= -->
  <!-- LOG PATTERN -->
  <!-- ========================= -->
  <!-- <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level
  %logger{36} - %msg%n" /> -->
  <property name="LOG_PATTERN"
    value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-NO_TRACE}] %logger{36} - %msg%n" />

  <!-- ========================= -->
  <!-- CONSOLE -->
  <!-- ========================= -->
  <appender name="CONSOLE"
    class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <!-- ========================= -->
  <!-- APP FILE (DAILY + SIZE, EXCLUDES ERROR) -->
  <!-- ========================= -->
  <appender name="APP_FILE"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>${LOG_PATH}/current/app.log</file>

    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>ERROR</level>
      <onMatch>DENY</onMatch>
      <onMismatch>ACCEPT</onMismatch>
      <!-- <onMismatch>NEUTRAL</onMismatch> -->
    </filter>

    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>
        ${LOG_PATH}/%d{yyyy-MM,aux}/app-%d{yyyy-MM-dd}.%i.log.gz
      </fileNamePattern>
      <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
      <maxHistory>${LOG_RETENTION_DAYS}</maxHistory>
      <totalSizeCap>${LOG_MAX_TOTAL_SIZE}</totalSizeCap>
      <cleanHistoryOnStart>true</cleanHistoryOnStart>
    </rollingPolicy>

    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <!-- ========================= -->
  <!-- ERROR FILE -->
  <!-- ========================= -->
  <appender name="ERROR_FILE"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>${LOG_PATH}/current/error.log</file>

    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>ERROR</level>
    </filter>

    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>
        ${LOG_PATH}/%d{yyyy-MM,aux}/error-%d{yyyy-MM-dd}.%i.log.gz
      </fileNamePattern>
      <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
      <maxHistory>${LOG_RETENTION_DAYS}</maxHistory>
      <totalSizeCap>${LOG_MAX_TOTAL_SIZE}</totalSizeCap>
      <cleanHistoryOnStart>true</cleanHistoryOnStart>
    </rollingPolicy>

    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <!-- ========================= -->
  <!-- ROOT -->
  <!-- ========================= -->
  <root level="INFO">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="APP_FILE" />
    <appender-ref ref="ERROR_FILE" />
  </root>

</configuration>